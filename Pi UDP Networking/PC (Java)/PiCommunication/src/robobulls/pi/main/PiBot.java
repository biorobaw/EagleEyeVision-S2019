package robobulls.pi.main;

import robobulls.pi.networking.PiBotBase;

/**
 * Example class that shows how to create a custom PiBot from PiBotBase
 */
class PiBot extends PiBotBase
{
    // Add additional fields as necessary

    PiBot(int id)
    {
        super(id);
    }
}